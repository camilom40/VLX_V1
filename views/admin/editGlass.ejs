<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/_head.ejs') %>
  <title>Edit Glass</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .input-padding {
      padding-left: 10px; /* Add padding inside the text input */
    }
    .bg-header-blue {
      background-color: #1e3a8a; /* Custom blue color */
    }
    .bg-header-blue:hover {
      background-color: #1e40af; /* Darker blue on hover */
    }
  </style>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">
  <%- include('../partials/_header.ejs') %>
  <div class="container mx-auto mt-10 flex-grow">
    <h1 class="text-3xl font-bold mb-5">Edit Glass</h1>
    <form action="/admin/glasses/update/<%= glass._id %>" method="POST" class="bg-white p-6 rounded-lg shadow-md">
      <div class="mb-4">
        <label for="glass_type" class="block text-sm font-medium text-gray-700">Glass Type</label>
        <input type="text" id="glass_type" name="glass_type" value="<%= glass.glass_type %>" required class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm input-padding">
      </div>
      <div class="mb-4">
        <label for="description" class="block text-sm font-medium text-gray-700">Description</label>
        <textarea id="description" name="description" required class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm input-padding"><%= glass.description %></textarea>
      </div>
      <div class="mb-4">
        <label for="missile_type" class="block text-sm font-medium text-gray-700">Missile Type</label>
        <select id="missile_type" name="missile_type" required class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm input-padding">
          <option value="LMI" <%= glass.missile_type === 'LMI' ? 'selected' : '' %>>LMI</option>
          <option value="SMI" <%= glass.missile_type === 'SMI' ? 'selected' : '' %>>SMI</option>
        </select>
      </div>
      <div class="mb-4">
        <label for="pricePerSquareMeter" class="block text-sm font-medium text-gray-700">Price Per Square Meter COP</label>
        <input type="number" step="0.01" id="pricePerSquareMeter" name="pricePerSquareMeter" value="<%= glass.pricePerSquareMeter %>" required class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm input-padding">
      </div>
      <div class="mb-4">
        <label for="weight" class="block text-sm font-medium text-gray-700">Weight Kg/m2</label>
        <input type="number" step="0.01" id="weight" name="weight" value="<%= glass.weight %>" required class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm input-padding">
      </div>
      <div class="flex items-center justify-between">
        <button type="submit" class="bg-header-blue hover:bg-header-blue text-white font-bold py-2 px-4 rounded focus:outline-none focus:ring-2 focus:ring-blue-500">Update Glass</button>
        <a href="/admin/glasses" class="text-blue-600 hover:text-blue-900">Cancel</a>
      </div>
    </form>
  </div>
  <%- include('../partials/_footer.ejs') %>
</body>
</html>
